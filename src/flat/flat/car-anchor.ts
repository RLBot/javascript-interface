// @ts-nocheck automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { Vector3, Vector3T } from '../../flat/flat/vector3.js';


/**
 * A RenderAnchor attached to a car.
 * The local field allows for an offset in local coordinates taking the car's orientation into account.
 */
export class CarAnchor implements flatbuffers.IUnpackableObject<CarAnchorT> {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):CarAnchor {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsCarAnchor(bb:flatbuffers.ByteBuffer, obj?:CarAnchor):CarAnchor {
  return (obj || new CarAnchor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsCarAnchor(bb:flatbuffers.ByteBuffer, obj?:CarAnchor):CarAnchor {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new CarAnchor()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

/**
 * The index of the car.
 */
index():number {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint32(this.bb_pos + offset) : 0;
}

/**
 * An offset in local coordinates.
 * x is forwards, y is left, and z is up.
 */
local(obj?:Vector3):Vector3|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? (obj || new Vector3()).__init(this.bb_pos + offset, this.bb!) : null;
}

static startCarAnchor(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addIndex(builder:flatbuffers.Builder, index:number) {
  builder.addFieldInt32(0, index, 0);
}

static addLocal(builder:flatbuffers.Builder, localOffset:flatbuffers.Offset) {
  builder.addFieldStruct(1, localOffset, 0);
}

static endCarAnchor(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  builder.requiredField(offset, 6) // local
  return offset;
}


unpack(): CarAnchorT {
  return new CarAnchorT(
    this.index(),
    (this.local() !== null ? this.local()!.unpack() : null)
  );
}


unpackTo(_o: CarAnchorT): void {
  _o.index = this.index();
  _o.local = (this.local() !== null ? this.local()!.unpack() : null);
}
}

export class CarAnchorT implements flatbuffers.IGeneratedObject {
constructor(
  public index: number = 0,
  public local: Vector3T|null = null
){}


pack(builder:flatbuffers.Builder): flatbuffers.Offset {
  CarAnchor.startCarAnchor(builder);
  CarAnchor.addIndex(builder, this.index);
  CarAnchor.addLocal(builder, (this.local !== null ? this.local!.pack(builder) : 0));

  return CarAnchor.endCarAnchor(builder);
}
}
